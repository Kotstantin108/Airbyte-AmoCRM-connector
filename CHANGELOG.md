# Changelog

## Очистка проекта для код-ревью

### Удалённые файлы (устаревшие, не используются)

- `amocrm_airbyte_manifest.yaml` — старый declarative manifest подход
- `spec_override.py` — использовался только с declarative manifest
- `entrypoint.sh` — использовался только с declarative manifest
- `components.py` — OAuth authenticator из declarative подхода (теперь используется `DatabaseTokenManager`)

### Изменения в коде

- Удалён неиспользуемый импорт `json` из `source.py`
- Удалён неиспользуемый импорт `Tuple` из типов
- Удалён закомментированный код логирования URL запросов
- Обновлён `README.md` — документация соответствует текущей Python CDK реализации

### Текущая структура проекта

```
.
├── .dockerignore          # Игнорируемые файлы для Docker
├── build.sh              # Скрипт сборки и публикации образа
├── Dockerfile            # Docker образ коннектора
├── main.py               # Точка входа Airbyte CDK
├── requirements.txt      # Python зависимости
├── README.md             # Документация проекта
├── source_amo_custom/    # Основной код коннектора
│   ├── __init__.py
│   └── source.py         # Логика коннектора (738 строк)
└── sql/                  # SQL скрипты
    └── triggers_is_deleted.sql  # Триггеры для обработки удалённых записей
```

### Готовность к код-ревью

✅ Все неиспользуемые файлы удалены  
✅ Код очищен от комментариев и неиспользуемых импортов  
✅ Документация актуальна  
✅ Структура проекта понятна  
✅ Зависимости минимальны и актуальны  
